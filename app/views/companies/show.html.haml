%h1= @company.name

%table
  %tbody
    %tr
      %th Project
      %th Company
    = render partial: 'projects/project', collection: @company.projects

= content_for :aside do
  = render 'sidebar'        