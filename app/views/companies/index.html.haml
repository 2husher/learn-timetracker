%h1 Companies:index

%p Here are all of the companies:

%table
  %tbody
    %tr
      %th Company
    - @companies.each do |company|
      %tr
        %td= link_to company, company

= content_for :aside do
  = render 'sidebar'