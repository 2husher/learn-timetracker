%h1 Projects:index

%p Here are all of the projects:

%table
  %tbody
    %tr
      %th Project
      %th Company
    = render partial: 'project', collection: @projects

= content_for :aside do
  = render 'sidebar'        